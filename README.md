# Recommendation Engine — Collaborative + Elasticsearch
Hybrid recommendation engine combining collaborative filtering and Elasticsearch.
### ■ Research Highlights
- **Matrix Factorisation**: Decomposes the user–item interaction matrix via ALS to produce latent user and it- **Elasticsearch BM25 Fallback**: Indexes item content (title, description, tags) and uses BM25 retrieval fo- **Algorithms Focus**: Computes NDCG@10 and hit-rate@5 in an offline split evaluation, showing hybrid re-ran**Tech Stack:** Python, `scikit-learn`, `Elasticsearch`.
